pipeline{
    agent any
    stages{
        stage('CI started'){
            steps{
                sh 'echo CI for ToDoList started'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}